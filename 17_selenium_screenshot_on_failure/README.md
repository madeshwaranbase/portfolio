# Project 17 - Screenshot on Failure

Introduces a pytest hook that captures a screenshot when a Selenium test fails.

```bash
pip install -r requirements.txt
pytest -v
```

Screenshots are saved under `screenshots/`.
