# Project 11 - Selenium Login Test

Basic browser automation using Selenium and pytest.

## Install

```bash
pip install -r requirements.txt
```

## Run

```bash
pytest -v
```

The test uses the public demo site `the-internet.herokuapp.com`.
